# ==============================================================================
# 2-GPU smoke: GRPO + GroupReplayBuffer FreshPER (GRPO-signal × age_decay)
# ==============================================================================
# Purpose: Fast smoke run for the new group-aware GRPO-signal FreshPER path.
# Algorithm: GRPO (group_size=4), ppo_epochs=1, train_steps_per_env_step=1
# Replay: GroupReplayBuffer, priority = (|reward| + eps) * exp(-age / age_decay)
# Steps: 400
# Hardware: 8x H100 (GPU 0-3: train+ref, GPU 4-7: vLLM infer)
# ==============================================================================
defaults:
  - ../../ROLL/examples/config/traj_envs@_here_
  - ../../ROLL/examples/config/deepspeed_zero@_here_
  - ../../ROLL/examples/config/deepspeed_zero2@_here_
  - ../../ROLL/examples/config/deepspeed_zero2_cpuoffload@_here_
  - ../../ROLL/examples/config/deepspeed_zero3@_here_
  - ../../ROLL/examples/config/deepspeed_zero3_cpuoffload@_here_

hydra:
  run:
    dir: .
  output_subdir: null

exp_name: "aime_grpo_2h100_freshper_grpo_signal_fmtm01_smoke_20step"
seed: 42
timestamp: ${oc.env:TRAINING_TIMESTAMP,${now:%Y%m%d_%H%M%S}}
base_output_dir: /mnt/data/u/maw0a/python_project/freshness_aware/experiments/grpo_aime/output
logging_dir: ${base_output_dir}/${timestamp}/logs
output_dir: ${base_output_dir}/${timestamp}
render_save_dir: ${base_output_dir}/${timestamp}/render
system_envs:
  USE_MODELSCOPE: '1'

deepspeed_zero2_optimizer_offload:
  train_micro_batch_size_per_gpu: auto
  bf16:
    enabled: true
  fp16:
    enabled: false
    loss_scale: 0
    initial_scale_power: 16
    loss_scale_window: 1000
    hysteresis: 2
    min_loss_scale: 1
  zero_optimization:
    stage: 2
    offload_optimizer:
      device: cpu
      pin_memory: true
    allgather_partitions: true
    allgather_bucket_size: 1.0e+9
    overlap_comm: true
    reduce_scatter: true
    reduce_bucket_size: 5.0e+8
    contiguous_gradients: true

track_with: wandb
tracker_kwargs:
  project: roll-grpo-replay-aime
  name: ${timestamp}_${exp_name}
  notes: "2-GPU smoke run for AIME GRPO FreshPER: grpo_signal priority, no off-policy filter, format_penalty=-0.1"
  tags: [agentic, roll, math, aime, grpo, replay, fresh_per, grpo_signal, group_level, no_filter, format_penalty_m01, 2gpu, smoke, 20step]
  mode: offline
  dir: ${base_output_dir}/${timestamp}/wandb

# Debug runs only need metrics; skip the mandatory final DeepSpeed checkpoint.
checkpoint_config: null

# =============================================================================
# TRAINING CONFIGURATION
# =============================================================================
num_gpus_per_node: 2

# Exercises rollout -> on-policy train -> replay push/train for multiple steps.
max_steps: 20
save_steps: 10000
logging_steps: 1
eval_steps: 5
resume_from_checkpoint: false

train_from_replay_only: false
batch_adjust_mode: auto
async_generation_ratio: 1

rollout_batch_size: 8
val_batch_size: 4
sequence_length: 2048

# ===================== GRPO CORE PARAMETERS =====================
advantage_clip: 0.2
ppo_epochs: 1
adv_estimator: "grpo"
init_kl_coef: 0.0
use_kl_loss: false
kl_loss_coef: 0
whiten_advantages: true
entropy_loss_coef: 0
max_grad_norm: 1.0

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================
pretrain: /mnt/data/u/maw0a/models/Qwen2.5-7B-Instruct
reward_pretrain: ${pretrain}

actor_train:
  model_args:
    attn_implementation: fa2
    disable_gradient_checkpointing: false
    dtype: bf16
    model_type: ~
  training_args:
    learning_rate: 1.0e-6
    weight_decay: 0
    per_device_train_batch_size: 1
    gradient_accumulation_steps: 2
    warmup_steps: 1
    lr_scheduler_type: cosine
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: deepspeed_train
    strategy_config: ${deepspeed_zero2_optimizer_offload}
  device_mapping: list(range(0,1))
  infer_batch_size: 1
  use_dynamic_batching_in_train: true
  max_tokens_per_microbatch_in_train: 8192
  sequence_length_round_in_train: 4
  use_dynamic_batching_in_infer: true
  max_tokens_per_microbatch_in_infer: 8192
  sequence_length_round_in_infer: 4

actor_infer:
  model_args:
    disable_gradient_checkpointing: true
    dtype: bf16
  generating_args:
    max_new_tokens: 128
    top_p: 0.99
    top_k: 100
    num_beams: 1
    temperature: 0.99
    num_return_sequences: 1
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: vllm
    strategy_config:
      gpu_memory_utilization: 0.82
      block_size: 16
      load_format: auto
  device_mapping: list(range(1,2))

reference:
  model_args:
    attn_implementation: fa2
    disable_gradient_checkpointing: true
    dtype: bf16
    model_type: ~
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: hf_infer
    strategy_config: ~
  device_mapping: list(range(0,1))
  infer_batch_size: 1
  use_dynamic_batching_in_infer: true
  max_tokens_per_microbatch_in_infer: 8192
  sequence_length_round_in_infer: 4

# =============================================================================
# ENVIRONMENT CONFIGURATION - AIME (GRPO group_size=4)
# =============================================================================
action_pattern: '\\boxed\{(.*?)\}'
think_action_pattern: '<think>(.*?)</think>\s*\\boxed\{(.*?)\}'
user_prompt_no_think_format: '\boxed{ [your answer] }'
user_prompt_think_format: '<think> [Your thoughts] </think> \boxed{ [your answer] }'

max_tokens_per_step: 128
max_actions_per_traj: 1

reward_normalization:
  grouping: traj_group_id
  method: mean_std

train_env_manager:
  format_penalty: -0.1
  max_env_num_per_worker: 8
  num_env_groups: 2
  group_size: 4
  tags: [AIME]
  num_groups_partition: [2]
  max_traj_per_env: 2

val_env_manager:
  max_env_num_per_worker: 4
  num_env_groups: 4
  group_size: 1
  tags: [AIME]
  num_groups_partition: [4]

custom_envs:
  AIME:
    env_type: "roll_math"
    max_steps: ${max_actions_per_traj}
    max_tokens_per_step: ${max_tokens_per_step}
    user_prompt_format: ${user_prompt_no_think_format}
    agent_system_template: |
      You are a math competition solver. Solve the given problem step by step.
      AIME answers are always integers from 000 to 999.
      Wrap your final answer as \boxed{your_integer_answer}.
    agent_template: |
      Problem:
      {observation}
      You have {actions_left} attempts remaining.
      Think carefully, then put only your final integer answer in \boxed{{}} within {max_response_length} tokens.
    reward_template: "Your previous answer was incorrect (reward: {reward}). Try again.\n"
    env_manager_cls: roll.pipeline.agentic.env_manager.traj_env_manager.TrajEnvManager
    use_thread_lock: true
    env_config:
      dataset_name: /mnt/data/u/maw0a/python_project/freshness_aware/datasets/math_benchmarks/aime_train.parquet
      split: "train"
      format_penalty: ${train_env_manager.format_penalty}

# =============================================================================
# OFF-POLICY MONITORING
# =============================================================================
offpolicy_monitor:
  enabled: true
  save_behavior_log_probs: true
  monitor_fresh_batch: true
  monitor_replay_batch: true
  monitor_interval: 1

# =============================================================================
# TRAJECTORY LOGGING
# =============================================================================
trajectory_log:
  enabled: true
  save_ratio: 0.1
  max_samples_per_step: 20
  filename: trajectory_samples.jsonl

# =============================================================================
# REPLAY BUFFER - Group-level GRPO-signal FreshPER
# =============================================================================
replay:
  enabled: true
  group_level: true              # MUST be true for GRPO group structure
  capacity: 16                 # 2000 groups × 4 = 8000 trajectories
  min_size: 2                  # Warmup: need 128 groups before replay starts
  train_steps_per_env_step: 1    # 1 extra replay training step per rollout
  use_rollout_batch_size: true
  sampling_mode: "trajectory"
  storage_mode: tokens_only
  lazy_tokenization: false
  eviction_strategy: "fifo"

  # GRPO-signal FreshPER: priority updated from group learning signal, then decayed by age.
  sample_method: "uniform"       # sample_method ignored when priority_function != lifo/fifo/uniform
  priority_function: "grpo_signal"
  priority_exponent: 0.3

  enable_age_decay: true         # KEY DIFF vs exp4: age decay ON
  age_decay: 20.0                # time constant in global_steps; exp(-age/20)
  refresh_interval: 1            # refresh age-decayed priorities every N training steps

  # Keep disabled for GRPO: trajectory-level filtering can break the sampled group.
  enable_offpolicy_filter: false
  ratio_clip_max: 3.0

  # Importance sampling correction to compensate for non-uniform sampling bias
  importance_sampling_correction: true
  importance_beta: 0.2
