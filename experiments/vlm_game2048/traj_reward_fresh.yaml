# ==============================================================================
# VLM GAME2048 OFF-POLICY - Trajectory Level + Reward-Fresh
# ==============================================================================
# Purpose: VLM with our Reward-Fresh priority extension
# Hardware: 4x H100
#   - GPU 0-1: actor_train + reference (DeepSpeed ZeRO-2)
#   - GPU 2-3: actor_infer (vLLM)
# Key Features:
#   - VLTrajEnvManager for multimodal (image + text) processing
#   - Reward-Fresh priority: (|reward| + e) x exp(-age/age_decay)
#   - Importance sampling correction
# ==============================================================================
defaults:
  - ../../ROLL/examples/config/traj_envs@_here_
  - ../../ROLL/examples/config/deepspeed_zero@_here_
  - ../../ROLL/examples/config/deepspeed_zero2@_here_
  - ../../ROLL/examples/config/deepspeed_zero2_cpuoffload@_here_
  - ../../ROLL/examples/config/deepspeed_zero3@_here_
  - ../../ROLL/examples/config/deepspeed_zero3_cpuoffload@_here_

hydra:
  run:
    dir: .
  output_subdir: null

exp_name: "vlm_game2048_traj_reward_fresh"
seed: 42
timestamp: ${oc.env:TRAINING_TIMESTAMP,${now:%Y%m%d_%H%M%S}}
base_output_dir: /mnt/project_modelware_roce/zhaojian/liangsirui/weiyu/projects/local_roll_dev/roll_dev/experiments/vlm_game2048/output
logging_dir: ${base_output_dir}/${timestamp}/logs
output_dir: ${base_output_dir}/${timestamp}
render_save_dir: ${base_output_dir}/${timestamp}/render
system_envs:
  USE_MODELSCOPE: '1'

track_with: wandb
tracker_kwargs:
  project: roll-vlm-game2048
  name: ${timestamp}_${exp_name}
  notes: "VLM Off-Policy: Game2048 4x4, Reward-Fresh (reward x age_decay), 4xH100"
  tags: [agentic, roll, game2048, vlm, replay, reward_fresh, trajectory, 4xH100]
  mode: offline
  dir: ${base_output_dir}/${timestamp}/wandb

checkpoint_config:
  type: file_system
  output_dir: ${base_output_dir}/${timestamp}/models/${exp_name}

# =============================================================================
# TRAINING CONFIGURATION
# =============================================================================

num_gpus_per_node: 4

max_steps: 100
save_steps: 10000
logging_steps: 1
eval_steps: 10
resume_from_checkpoint: false

train_from_replay_only: false
batch_adjust_mode: auto
async_generation_ratio: 1

rollout_batch_size: 128
val_batch_size: 128
sequence_length: 4096

advantage_clip: 20
ppo_epochs: 1
adv_estimator: "reinforce"
init_kl_coef: 0.1
use_kl_loss: true
kl_loss_coef: 0.05
whiten_advantages: true
entropy_loss_coef: 0.01
max_grad_norm: 1.0

# =============================================================================
# MODEL CONFIGURATION - VLM
# =============================================================================

pretrain: /mnt/project_modelware_roce/zhaojian/models/pretrain/Qwen2.5-VL-3B-Instruct
reward_pretrain: ${pretrain}

actor_train:
  model_args:
    attn_implementation: fa2
    disable_gradient_checkpointing: false
    dtype: bf16
    model_type: ~
  training_args:
    learning_rate: 1.0e-6
    weight_decay: 0
    per_device_train_batch_size: 1
    gradient_accumulation_steps: 32
    warmup_steps: 10
    lr_scheduler_type: cosine
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: deepspeed_train
    strategy_config: ${deepspeed_zero2}
  device_mapping: list(range(0,2))
  infer_batch_size: 1
  use_dynamic_batching_in_train: true
  max_tokens_per_microbatch_in_train: 16384
  sequence_length_round_in_train: 4
  use_dynamic_batching_in_infer: true
  max_tokens_per_microbatch_in_infer: 16384
  sequence_length_round_in_infer: 4

actor_infer:
  model_args:
    disable_gradient_checkpointing: true
    dtype: bf16
  generating_args:
    max_new_tokens: 128
    top_p: 0.99
    top_k: 100
    num_beams: 1
    temperature: 0.99
    num_return_sequences: 1
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: vllm
    strategy_config:
      gpu_memory_utilization: 0.85
      block_size: 16
      load_format: auto
      limit_mm_per_prompt:
        image: ${max_actions_per_traj}
  device_mapping: list(range(2,4))

reference:
  model_args:
    attn_implementation: fa2
    disable_gradient_checkpointing: true
    dtype: bf16
    model_type: ~
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: hf_infer
    strategy_config: ~
  device_mapping: list(range(0,2))
  infer_batch_size: 1
  use_dynamic_batching_in_infer: true
  max_tokens_per_microbatch_in_infer: 16384
  sequence_length_round_in_infer: 4

# =============================================================================
# ENVIRONMENT CONFIGURATION - VLM Game2048
# =============================================================================

action_pattern: <answer>(.*?)</answer>
think_action_pattern: <think>(.*?)</think>\s*<answer>(.*?)</answer>
user_prompt_no_think_format: <answer> [your answer] </answer>
user_prompt_think_format: <think> [Your thoughts] </think> <answer> [your answer] </answer>

max_tokens_per_step: 128
max_actions_per_traj: 15

reward_normalization:
  grouping: traj_group_id
  method: mean_std

train_env_manager:
  format_penalty: -0.15
  max_env_num_per_worker: 16
  num_env_groups: 32
  group_size: 4
  tags: [Game2048]
  num_groups_partition: [32]
  max_traj_per_env: 2

val_env_manager:
  max_env_num_per_worker: 32
  num_env_groups: 128
  group_size: 1
  tags: [Game2048]
  num_groups_partition: [128]

custom_envs:
  Game2048:
    env_type: game2048
    max_tokens_per_step: ${max_tokens_per_step}
    user_prompt_format: ${user_prompt_no_think_format}
    env_manager_cls: roll.pipeline.agentic.env_manager.vl_traj_env_manager.VLTrajEnvManager

    agent_system_template: |
      You are playing the 2048 game by viewing images of the board.
      The image shows a 4x4 grid with numbered tiles.
      Tiles slide in the chosen direction and merge when two tiles of the same number collide.
      After each move, a new tile (2 or 4) appears randomly.
      Your goal is to create high-value tiles, especially reaching 2048.
      Output your action as: <answer>Direction</answer>
      Available actions: Up, Down, Left, Right

    pre_step_template: "Turn {turn_idx}:\nObserve the current board:"
    next_step_template: |
      You have {actions_left} actions left.
      Analyze the board image and decide your next move.
      Respond within {max_response_length} tokens.
    reward_template: "Previous action reward: {reward}\n"

    use_thread_lock: true

    env_config:
      env_instruction: |
        You are playing the 2048 game. Slide numbered tiles on a 4x4 grid.
        Tiles with the same number merge when they collide.
        After each move, a new tile (2 or 4) appears randomly.
        The goal is to create high-value tiles.
        Available actions: Up, Down, Left, Right.
        Format your answer as: <answer>Right</answer>
      action_pattern: ${action_pattern}
      max_steps: ${max_actions_per_traj}
      board_size: 4
      render_mode: "rgb_array"
      image_resolution: 256

# =============================================================================
# TRAJECTORY LOGGING
# =============================================================================

trajectory_log:
  enabled: true
  save_ratio: 0.1
  max_samples_per_step: 20
  filename: trajectory_samples.jsonl

# =============================================================================
# OFF-POLICY MONITORING
# =============================================================================

offpolicy_monitor:
  enabled: true
  save_behavior_log_probs: true
  monitor_fresh_batch: true
  monitor_replay_batch: true
  monitor_interval: 1

# =============================================================================
# REPLAY BUFFER - Reward-Fresh Configuration
# =============================================================================

replay:
  enabled: true
  capacity: 50000
  min_size: ${rollout_batch_size}
  train_steps_per_env_step: 2
  use_rollout_batch_size: true
  sampling_mode: "trajectory"
  storage_mode: tokens_only
  lazy_tokenization: false
  eviction_strategy: "fifo"

  # Reward-Fresh: priority = (|reward| + e) x exp(-age/age_decay)
  priority_function: "reward_fresh"
  priority_exponent: 0.6
  age_decay: 1000.0

  # Importance Sampling Correction
  importance_sampling_correction: true
  importance_beta: 0.4
